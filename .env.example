# Copy this file to .env and adjust values as needed for local development
# cp .env.example .env

# Postgres
POSTGRES_USER=ruze
POSTGRES_PASSWORD=ruze
POSTGRES_DB=ruze
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# pgweb (DB admin UI, optional via profile)
PGWEB_PORT=8081

